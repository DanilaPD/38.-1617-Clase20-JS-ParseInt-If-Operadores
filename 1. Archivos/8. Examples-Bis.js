const nacionalidad = "brasilero";

console.log(
  nacionalidad === "argentino"
    ? "Tu documento empieza en 32"
    : "Tu documento empieza en 92"
);

// Ternario es para casos que son VERDAD o MENTIRA.

// ¿Ya sabés si es true o false? ¡Entonces no necesitamos la comparación!
const helado = true;

console.log(helado ? "Wiii" : "Buuu");

const login = true;
console.log(login ? "Redirigir a cart." : "Redirigir a LogIn.");
